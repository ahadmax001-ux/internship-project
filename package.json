{
  "name": "atp-score-management-system",
  "version": "1.0.0",
  "description": "ATP Score Management & Calculation Web Application - MERN Stack College Project",
  "main": "server/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev",
    "start": "npm --prefix server start",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix client run build"
  },
  "keywords": [
    "mern",
    "atp-score",
    "calculator",
    "react",
    "express",
    "mongodb",
    "tailwind"
  ],
  "author": "College Student / Project Team",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
